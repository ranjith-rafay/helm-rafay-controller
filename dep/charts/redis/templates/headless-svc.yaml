apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-headless" (include "common_redis.names.fullname" .) }}
  namespace: {{ .Values.namespace | quote }}
  labels: {{- include "common_redis.labels.standard" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common_redis.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  annotations:
    {{- if .Values.sentinel.service.headless.annotations }}
    {{- include "common_redis.tplvalues.render" ( dict "value" .Values.sentinel.service.headless.annotations "context" $) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
    {{- include "common_redis.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
  {{- include "redis.externalDNS.annotations" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  {{- if .Values.sentinel.enabled }}
  publishNotReadyAddresses: true
  {{- end }}
  ports:
    - name: tcp-redis
      port: {{ if .Values.sentinel.enabled }}{{ .Values.sentinel.service.ports.redis }}{{ else }}{{ .Values.master.service.ports.redis }}{{ end }}
      targetPort: redis
    {{- if .Values.sentinel.enabled }}
    - name: tcp-sentinel
      port: {{ .Values.sentinel.service.ports.sentinel }}
      targetPort: redis-sentinel
    {{- end }}
  selector: {{- include "common_redis.labels.matchLabels" . | nindent 4 }}
