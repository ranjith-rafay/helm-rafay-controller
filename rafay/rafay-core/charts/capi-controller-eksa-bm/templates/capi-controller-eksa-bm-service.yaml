apiVersion: v1
kind: Service
metadata:
  name: capi-controller-eksa-bm
spec:
  ports:
    - name: grpc
      port: 7000
      protocol: TCP
      targetPort: 7000
    - name: api
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: capi-controller-eksa-bm
  sessionAffinity: None
  type: ClusterIP