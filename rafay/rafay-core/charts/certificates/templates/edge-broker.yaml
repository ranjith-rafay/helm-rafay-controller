apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: edge-selfsigned-cert
spec:
  dnsNames:
    - edge.core.{{ .Values.global.default_partner_console_domain }}
  secretName: selfsigned-cert-tls-edge
  duration: 87600h
  renewBefore: 12h
  issuerRef:
    name: self-signed-infar-ca-issuer
