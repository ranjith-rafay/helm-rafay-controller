apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: root-ca
spec:
  commonName: infra_ca
  isCA: true
  secretName: infra-ca-tls
  duration: 87600h
  renewBefore: 12h
  issuerRef:
    name: {{ .Values.global.issuer_name | default "self-signed-ca-cert" }}
    kind: ClusterIssuer
